import React from 'react';
import SpokoStoryDay from './SpokoStoryDay';

export default function SpokoStoryDay6({ activeTab, onNavigate, session }) {
  return <SpokoStoryDay day={6} activeTab={activeTab} onNavigate={onNavigate} session={session} />;
}
