import React from 'react';
import SpokoStoryDay from './SpokoStoryDay';

export default function SpokoStoryDay5({ activeTab, onNavigate, session }) {
  return <SpokoStoryDay day={5} activeTab={activeTab} onNavigate={onNavigate} session={session} />;
}
