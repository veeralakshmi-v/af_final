import React from 'react';
import SpokoStoryDay from './SpokoStoryDay';

export default function SpokoStoryDay4({ activeTab, onNavigate, session }) {
  return <SpokoStoryDay day={4} activeTab={activeTab} onNavigate={onNavigate} session={session} />;
}
